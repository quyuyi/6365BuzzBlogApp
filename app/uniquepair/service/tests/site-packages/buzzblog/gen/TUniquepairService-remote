#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.13.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
if sys.version_info[0] > 2:
    from urllib.parse import urlparse
else:
    from urlparse import urlparse
from thrift.transport import TTransport, TSocket, TSSLSocket, THttpClient
from thrift.protocol.TBinaryProtocol import TBinaryProtocol

from gen import TUniquepairService
from gen.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
    print('')
    print('Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] [-s[sl]] [-novalidate] [-ca_certs certs] [-keyfile keyfile] [-certfile certfile] function [arg1 [arg2...]]')
    print('')
    print('Functions:')
    print('  TUniquepair get(i32 uniquepair_id)')
    print('  TUniquepair add(string domain, i32 first_elem, i32 second_elem)')
    print('  void remove(i32 uniquepair_id)')
    print('  TUniquepair find(string domain, i32 first_elem, i32 second_elem)')
    print('   all(string domain)')
    print('   filter_by_first_elem(string domain, i32 first_elem)')
    print('   filter_by_second_elem(string domain, i32 second_elem)')
    print('  i32 count_first_elem(string domain, i32 first_elem)')
    print('  i32 count_second_elem(string domain, i32 second_elem)')
    print('')
    sys.exit(0)

pp = pprint.PrettyPrinter(indent=2)
host = 'localhost'
port = 9090
uri = ''
framed = False
ssl = False
validate = True
ca_certs = None
keyfile = None
certfile = None
http = False
argi = 1

if sys.argv[argi] == '-h':
    parts = sys.argv[argi + 1].split(':')
    host = parts[0]
    if len(parts) > 1:
        port = int(parts[1])
    argi += 2

if sys.argv[argi] == '-u':
    url = urlparse(sys.argv[argi + 1])
    parts = url[1].split(':')
    host = parts[0]
    if len(parts) > 1:
        port = int(parts[1])
    else:
        port = 80
    uri = url[2]
    if url[4]:
        uri += '?%s' % url[4]
    http = True
    argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
    framed = True
    argi += 1

if sys.argv[argi] == '-s' or sys.argv[argi] == '-ssl':
    ssl = True
    argi += 1

if sys.argv[argi] == '-novalidate':
    validate = False
    argi += 1

if sys.argv[argi] == '-ca_certs':
    ca_certs = sys.argv[argi+1]
    argi += 2

if sys.argv[argi] == '-keyfile':
    keyfile = sys.argv[argi+1]
    argi += 2

if sys.argv[argi] == '-certfile':
    certfile = sys.argv[argi+1]
    argi += 2

cmd = sys.argv[argi]
args = sys.argv[argi + 1:]

if http:
    transport = THttpClient.THttpClient(host, port, uri)
else:
    if ssl:
        socket = TSSLSocket.TSSLSocket(host, port, validate=validate, ca_certs=ca_certs, keyfile=keyfile, certfile=certfile)
    else:
        socket = TSocket.TSocket(host, port)
    if framed:
        transport = TTransport.TFramedTransport(socket)
    else:
        transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol(transport)
client = TUniquepairService.Client(protocol)
transport.open()

if cmd == 'get':
    if len(args) != 1:
        print('get requires 1 args')
        sys.exit(1)
    pp.pprint(client.get(eval(args[0]),))

elif cmd == 'add':
    if len(args) != 3:
        print('add requires 3 args')
        sys.exit(1)
    pp.pprint(client.add(args[0], eval(args[1]), eval(args[2]),))

elif cmd == 'remove':
    if len(args) != 1:
        print('remove requires 1 args')
        sys.exit(1)
    pp.pprint(client.remove(eval(args[0]),))

elif cmd == 'find':
    if len(args) != 3:
        print('find requires 3 args')
        sys.exit(1)
    pp.pprint(client.find(args[0], eval(args[1]), eval(args[2]),))

elif cmd == 'all':
    if len(args) != 1:
        print('all requires 1 args')
        sys.exit(1)
    pp.pprint(client.all(args[0],))

elif cmd == 'filter_by_first_elem':
    if len(args) != 2:
        print('filter_by_first_elem requires 2 args')
        sys.exit(1)
    pp.pprint(client.filter_by_first_elem(args[0], eval(args[1]),))

elif cmd == 'filter_by_second_elem':
    if len(args) != 2:
        print('filter_by_second_elem requires 2 args')
        sys.exit(1)
    pp.pprint(client.filter_by_second_elem(args[0], eval(args[1]),))

elif cmd == 'count_first_elem':
    if len(args) != 2:
        print('count_first_elem requires 2 args')
        sys.exit(1)
    pp.pprint(client.count_first_elem(args[0], eval(args[1]),))

elif cmd == 'count_second_elem':
    if len(args) != 2:
        print('count_second_elem requires 2 args')
        sys.exit(1)
    pp.pprint(client.count_second_elem(args[0], eval(args[1]),))

else:
    print('Unrecognized method %s' % cmd)
    sys.exit(1)

transport.close()
